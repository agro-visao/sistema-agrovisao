name = "agrovisao-site"
compatibility_date = "2025-01-01"
pages_build_output_dir = "./dist"

# Banco de dados, storage e autenticação agora são Supabase (Postgres + Storage
# + Auth). As Pages Functions acessam via @supabase/supabase-js com as
# variáveis SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY (ver .env.example).
# Sem bindings nativos do Cloudflare a configurar aqui.
